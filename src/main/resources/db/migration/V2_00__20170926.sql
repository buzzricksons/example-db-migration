CREATE TABLE TCITY2 (
    code VARCHAR(255),
    name VARCHAR(255),
    state VARCHAR(255),
    country VARCHAR(255),
    CONSTRAINT CITY2_PK PRIMARY KEY (code)
);

INSERT INTO tcity2 (code, name, state, country) VALUES ('asdfjljklk234kj234', 'San Francisco', 'CA', 'US');